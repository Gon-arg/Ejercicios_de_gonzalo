/*Escriba un programa que permita al usuario ingresar las medidas de 2 lados de un
rectángulo, y que luego imprima por pantalla un rectángulo vacío (sin relleno), es
decir, solo debe dibujar los lados.*/
/*int largo;
int ancho;
bool numero_valido;
do
{
    Console.WriteLine("Ingrese el ancho:");
    numero_valido = int.TryParse(Console.ReadLine(), out ancho);
} while (!(numero_valido && ancho > 0));
do
{
    Console.WriteLine("Ingrese el largo:");
    numero_valido = int.TryParse(Console.ReadLine(), out largo);
} while (!(numero_valido && largo > 0));

for (int i = 0; i < ancho; i++)
{
    for (int j = 0; j < largo; j++)
    {
        if (i == 0 || i == ancho - 1 || j == 0 || j == largo - 1)
        {
            Console.Write("* ");
        }
        else
        {
            Console.Write("  "); // dos espacios para alinear con "* "
        }
    }
    Console.WriteLine();
}*/
